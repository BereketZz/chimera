
# Skill: Generate Content

**Purpose:** Create persona-consistent social media content (text, image, video) using context from trends and past memory.

**Input Schema:**
```json
{
  "persona_id": "string",
  "topic": "string",
  "trend_data": ["string"],
  "media_reference_id": ["string (optional)"]
}